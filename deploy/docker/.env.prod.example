# Production Environment Configuration for Luthien Proxy
# Copy this file to .env and fill in your values
#
# SECURITY: All values marked [REQUIRED] must be set before deployment!
# SECURITY: Use strong, randomly generated passwords/keys for production!

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================

# Your domain name (required for HTTPS)
# Example: luthien.example.com
DOMAIN_NAME=your.domain.com

# =============================================================================
# AUTHENTICATION [REQUIRED]
# =============================================================================

# API key that clients use to authenticate to the proxy
# Generate with: openssl rand -hex 32
PROXY_API_KEY=

# API key for admin/policy management operations
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=

# =============================================================================
# LLM PROVIDER API KEYS
# =============================================================================

# At least one of these is required for the proxy to work
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# =============================================================================
# DATABASE [REQUIRED]
# =============================================================================

# PostgreSQL credentials
# Generate password with: openssl rand -base64 24
POSTGRES_USER=luthien
POSTGRES_PASSWORD=
POSTGRES_DB=luthien_control

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Policy configuration
POLICY_SOURCE=db-fallback-file

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# OpenTelemetry (set to true and configure OTEL_EXPORTER_OTLP_ENDPOINT if using)
OTEL_ENABLED=false
# OTEL_EXPORTER_OTLP_ENDPOINT=http://your-tempo:4317
