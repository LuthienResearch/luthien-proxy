# Stale Test Doc

This file is a planted doc-drift artifact for testing the nightly autofix
pipeline. It will be removed shortly after the test.

Run `./scripts/nightly/this_does_not_exist.sh` to do nothing — it is not a
real script.

The function `luthien_proxy.this_module_does_not_exist.run()` is also a
fabricated reference that should be flagged by the doc-drift sweep.
